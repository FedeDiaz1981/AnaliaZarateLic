<section id="contacto" class="border-t bg-slate-50">
  <div class="mx-auto max-w-6xl px-6 py-16">
    <h2 class="text-2xl font-semibold">Contacto</h2>
    <p class="mt-2 max-w-2xl text-slate-600">
      Formulario básico. Si no vas a procesar backend todavía, dejalo “dummy”.
    </p>

    <form class="mt-8 grid gap-4 max-w-xl">
      <input class="h-11 rounded-xl border px-4" placeholder="Nombre" />
      <input class="h-11 rounded-xl border px-4" placeholder="Email" type="email" />
      <textarea class="min-h-[120px] rounded-xl border p-4" placeholder="Mensaje"></textarea>

      <button
        type="button"
        class="h-11 rounded-xl bg-brand-500 px-4 text-sm font-semibold text-white hover:opacity-90"
      >
        Enviar
      </button>
    </form>
  </div>
</section>
